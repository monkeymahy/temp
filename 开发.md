

## Log

* 20251029：1）首次提交到gitlab；2）生成添加requirements.txt文件；初筛完毕训练代码直至模型初始化结束；3）将wandb替换为swanlab；初筛到模型构造代码结束；

## Bug List

* [ ] layers.py文件中self.mlp=MLP这里存在参数传递错误bug；
* [ ] AAGNetGraphEncoder初始化函数的self.linear=MLP实例化时hidden_dim传0了；

## TODO List

1. BaseDataset类的load_graphs()方法内，需要后续判断threading的效率是否最优。CPU 密集型任务：multiprocessing 更快；I/O 密集型任务：threading 通常足够且开销更小。
2. load_json_or_pkl()函数非常占用内存，峰值80G，稳定在45G；比如转存成其他更高效的格式；
3. node属性、node网格、egde属性、edge网格是什么？
4. MPNN是什么层？
5. PNAConvTower是什么层？
6. 什么是node grid（在AAGNetSegmentor的初始化函数中被提及）？
7. wandb未激活，切换成swanlab；
8. 目前依赖库有一个Windows平台独享的包；
9. 代码无法在IDE启动；
10. 从Windows迁移到ubuntu；

## 环境

```python
# 保存python环境
pip list --format=freeze > requirements.txt
```
